#include "async/thread_pool.h"
#include <future>
#include <gtest/gtest.h>

TEST(ThreadPoolTest, SubmitTask) {
  size_t sz = 10;
  ThreadPool tp = ThreadPool(sz);
  std::future<int> res = tp.submit([](int a, int b) { return a + b; }, 10, 12);

  EXPECT_EQ(res.get(), 22);
}
